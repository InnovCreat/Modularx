[package]
name = "nexus-parser"
description = "Pratt parser for the NEXUS compiler"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
nexus-span = { path = "../nexus-span" }
nexus-token = { path = "../nexus-token" }

[dev-dependencies]
nexus-lexer = { path = "../nexus-lexer" }
