[package]
name = "modularis-demo"
version = "0.1.0"
edition = "2021"
description = "Live demo — MODULARIS orchestrator with NEXUS compiler, SLTQ, and Vortex"

[dependencies]
modularis = { path = "../modularis" }
